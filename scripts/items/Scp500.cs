using Godot;

[GlobalClass]
public partial class Scp500 : InventoryItem
{
    /*
    public override void PrimaryPress(NetworkPlayer player)
    {
        StatusEffectManager.Instance.RemoveAllNegativeEffects(player);
        player.Heal(new HealInfo(9999999));
        player.InventorySerials[player.EquippedItemIndex] = -1;
        player.EquippedItemIndex = -1;
    }
    */
}